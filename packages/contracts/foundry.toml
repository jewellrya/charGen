[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
evm_version = "paris"

# so you can: import "@openzeppelin/contracts/..."
remappings = [
  "@openzeppelin/=lib/openzeppelin-contracts/"
]